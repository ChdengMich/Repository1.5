import { Building } from '../../types'

export interface Object3DProps {
    position?: [number, number, number]
    rotation?: [number, number, number]
    scale?: [number, number, number]
}

export { Building }
export { buildings, roads, decorations } from '../../types' 